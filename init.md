# Initialize Blitz

```sh
$ mkdir blitz-example
$ cd blitz-example
$ docker run --rm -it -p 3000:3000 -v $(pwd):/myapp -w /myapp node:15-alpine /bin/sh
# apk add --no-cache libtool autoconf automake build-base python3 git gnupg
# git config --global user.email "<email>"
# git config --global user.name "<name>"
# npm install -g blitz
# blitz new .
# blitz start
```
